{
  "name": "julius-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local, evidence-aware context optimization and economy ledger",
  "scripts": {
    "start": "bun src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/cli.ts --compile --outfile dist/julius",
    "smoke": "bun scripts/smoke.ts"
  },
  "devDependencies": { "@types/bun": "latest", "typescript": "~5.9.3" }
}
